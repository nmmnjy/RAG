# 05 实施记录

## 当前状态
- 状态：进行中（已完成设计 + 代码骨架 + 基础测试骨架）

## 本线程新增/修改内容
- 新增答案生成 schema：`backend/app/schemas/answer_generation.py`
  - 定义 `AnswerGenerationRequest` / `AnswerGenerationResult`
  - 定义 `AnswerCitation`、`LLMGenerateRequest`、`LLMGenerateResult`
- 新增可替换 LLM provider 接口：`backend/app/services/llm_provider.py`
  - 抽象接口 `LLMProvider`
  - 默认实现 `MockLLMProvider`
- 新增拒答策略骨架：`backend/app/services/refusal_policy.py`
  - 空上下文拒答：`QA_CONTEXT_EMPTY`
  - 证据不足拒答：`QA_EVIDENCE_INSUFFICIENT`
- 新增引用拼接器：`backend/app/services/citation_builder.py`
  - 基于 04 `HybridRetrieveHit` 构造前端可消费 `citations`
- 新增答案生成流程骨架：`backend/app/services/answer_generation_service.py`
  - 直接消费 04 `HybridRetrieveResult`
  - 串联拒答、上下文构建、provider 调用、citation 拼接、confidence 计算
- 新增基础单测：`backend/tests/unit/test_answer_generation_service.py`
  - 稳定字段输出断言
  - 证据不足拒答断言
  - 空命中拒答断言

## 共享文档更新
- 新增规范：`docs/specs/05_答案生成与溯源.md`
  - 明确 05 输入/输出契约
  - 明确 citation 结构、provider 接口、拒答规则与默认流程
- 新增实施记录：`docs/changes/05_实施记录.md`（本文）

## 测试/验证结果
- 已新增 05 单测骨架，覆盖：
  - 正常可答场景
  - 证据不足拒答
  - 空上下文拒答
- 待执行 `pytest` 进行本地验证（见后续执行结果）。

## 风险与待确认项
- 风险：
  - 当前为 mock/noop 生成，未覆盖真实 LLM 生成质量与延迟表现。
  - confidence 为规则融合值，未经过评测标定。
- 待确认项：
  - `QA_EVIDENCE_INSUFFICIENT` 是否纳入 00 全局错误码规范。
  - citation 是否在 04/05 基线层面强制页码字段。
