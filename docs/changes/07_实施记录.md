# 07 实施记录

## 当前状态
- 状态：进行中（已完成设计 + 工程骨架 + 可运行最小流程 + 基础测试骨架）

## 本线程新增/修改内容
- 新增评测契约 schema：`backend/app/schemas/evaluation.py`
  - 定义统一输入：`OfflineEvaluationDatasetInput`、`OfflineEvaluationCaseInput`
  - 定义统一输出：`EvaluationCheckResult`、`OfflineEvaluationReport`
  - 定义门禁规则：`ReleaseGateRule`、`ReleaseGateResult`
- 新增离线评测执行骨架：`backend/app/services/offline_evaluator.py`
  - 基于 01~05 现有服务串联执行（结构化 -> 分块 -> 检索 -> 答案）
  - 覆盖四类评测维度基础检查
  - 产出 case 级与 dataset 级汇总报告
  - 实现默认发布门禁规则
- 新增本地执行入口：`backend/app/evaluation/run_offline_evaluation.py`
  - 支持 `--dataset` 输入与 `--output` 输出
  - 可直接生成 `evaluation/reports/*.json`
- 新增评测数据集目录骨架：
  - `evaluation/datasets/README.md`
  - `evaluation/datasets/sample_v1/manifest.json`
  - `evaluation/datasets/sample_v1/{cases,expected,snapshots}/.gitkeep`
  - `evaluation/reports/.gitkeep`
- 新增基础测试骨架：
  - `backend/tests/unit/test_evaluation_schema.py`
  - `backend/tests/unit/test_offline_evaluator.py`

## 共享文档更新
- 新增共享规范：`docs/specs/07_评测体系与验收执行.md`
  - 明确 07 输入/输出契约
  - 明确四类评测维度基础检查
  - 明确验收报告结构与门禁规则骨架
  - 明确本地可运行流程

## 对外契约确认
- 07 评测模块直接消费并复用上游冻结契约：
  - `01` `StructuredDocument`
  - `02` `ChunkBuildResult`
  - `04` `HybridRetrieveResult`
  - `05` `AnswerGenerationResult`
- 未新增与 00 冻结字段冲突的业务命名。

## 测试/验证结果
- 新增 `07` 单测骨架并通过（见本轮 `pytest` 执行结果）。
- 已验证本地命令可从样例数据集生成报告文件：
  - `python -m app.evaluation.run_offline_evaluation --dataset ..\\evaluation\\datasets\\sample_v1\\manifest.json`

## 风险与未覆盖项
- 当前为样例数据 + mock provider + 规则评测，未覆盖真实线上分布与时延指标。
- 当前门禁仅实现最小规则骨架（通过率与维度必过），未覆盖趋势回归与多版本对比。
- 当前报告输出为离线 JSON 文件，未接入统一任务平台与可视化看板。

## 待确认项（仅提出）
- 是否在 07 阶段要求强制接入真实性能指标（P95 延迟/可用性）并纳入门禁。
- 是否要求报告字段纳入全局链路追踪字段（`request_id/trace_id`）以对齐线上日志体系。
