# 01 实施记录

## 当前状态
- 状态：进行中（已完成设计 + 代码骨架）

## 本轮实现
- 新增后端模块骨架：`backend/app`、`backend/tests`、`backend/.env.example`、`backend/requirements.txt`
- 实现文档解析 API 骨架：`/api/v1/document-parse/tasks` 创建/执行/查询/列表
- 实现多格式解析适配器接口与注册中心：PDF/Word/Excel/Markdown/TXT
- 定义结构化输出 schema：`StructuredDocument/StructuredSection/StructuredBlock`
- 实现解析任务状态机骨架：`pending/parsing/succeeded/failed/retryable`
- 实现基础测试骨架：API、状态机、schema
- 更新共享规范文档：`docs/specs/01_文档解析与结构化.md`
- 修复请求校验异常处理：改用 FastAPI `RequestValidationError`，非法请求稳定返回 `HTTP 400 + COMMON_INVALID_ARGUMENT + 统一错误结构`
- 补充 API 测试：覆盖非法请求体场景（非法 `format`）

## 对外变更
- 对 `02` 模块可直接消费的结构化输出对象已冻结（字段命名对齐 00 基线）
- 新增 01 模块 API 路径草案与任务状态语义
- 新增重试参数约束：`DOC_PARSE_MAX_RETRY`

## 风险与阻塞
- 当前解析适配器为占位实现，尚未接入真实文件抽取库，复杂版式解析能力未覆盖
- 仓储层当前为内存实现，服务重启会丢失任务状态
- 尚未接入网关透传时，`request_id/trace_id` 由服务端兜底生成

## 下一步
- 接入真实解析库并补齐各格式标准/边界/异常样例
- 将任务仓储替换为持久化实现（对齐后续数据库线程）
- 与 02 模块联调 `StructuredDocument -> chunk` 输入映射细节

## 本次修复验证结果
- 在 `backend` 目录执行 `pytest`，新增非法请求体测试通过
- 当前已验证非法请求返回结构包含：`code`, `message`, `details`, `request_id`, `trace_id`
