# 00 实施记录

## 当前状态
- 状态：已完成本轮主线程收口验收与冻结（2026-03-29）。

## 本轮执行内容
- 完成全仓一致性复核：核对 `docs/specs` 与 `docs/changes` 的 `00~08` 覆盖情况，并抽检实现与文档契约一致性。
- 执行统一验收命令：
  - `powershell -ExecutionPolicy Bypass -File .\infra\scripts\local-acceptance.ps1 -EvalDataset qa_demo_v1`
- 形成冻结清单并回写 00 规范文档。

## 一致性复核结论（00~08）
- `docs/specs`：`00~08` 文档齐全。
- `docs/changes`：`01~08` 已存在；`00_实施记录.md` 本轮补齐。
- 契约抽检通过：
  - 后端 schema 与 00 冻结字段保持一致（`doc_id/kb_id/chunk_id/score_vector/score_keyword/score_final/answer/citations/confidence/refuse_reason`）。
  - 前后端 API 前缀一致（`/api/v1`），问答接口路径一致（`/api/v1/qa/answers`）。
  - 未发现仓库中仍在使用 `ops/` 的实现路径（仅文档迁移说明中保留历史描述）。

## 统一验收结果
- 验收命令：`powershell -ExecutionPolicy Bypass -File .\infra\scripts\local-acceptance.ps1 -EvalDataset qa_demo_v1`
- 执行结果：通过（Exit Code `0`）
  - Frontend build：通过（Next.js build 成功）
  - Backend key tests：通过（`11 passed`）
  - Offline evaluation：通过（`release_blocked=False`）
  - 报告输出：`evaluation/reports/qa_demo_v1_20260329T052541Z.json`

## 本轮冻结清单
- 通过模块：`00, 01, 02, 03, 04, 05, 06, 07, 08`
- 未通过模块：无
- 风险与待下一轮项：
  - 真实 provider 端到端能力仍依赖真实密钥与外部可用性，当前通过结果包含 mock/fallback 路径。
  - 评测集规模仍偏小，`qa_demo_v1` 仅代表最小可运行验收，不代表生产质量上限。
  - CI 侧尚未强制执行本地验收脚本同等门禁，建议下轮纳入流水线。

## 影响文件
- 新增：`docs/changes/00_实施记录.md`
- 更新：`docs/specs/00_项目框架与规范.md`
