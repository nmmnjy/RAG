# 03 实施记录

## 当前状态
- 状态：进行中（第二轮已完成“真实接入层抽象升级”骨架）

## 本轮实现
- 新增向量化与向量存储 schema：`backend/app/schemas/vectorization.py`
  - 定义 `VectorizationInput`（直接继承 02 的 `ChunkBuildResult`）
  - 定义 `VectorRecord`、`VectorWriteRequest/Result`、`VectorQueryRequest/Hit`
  - 定义写入模式 `VectorWriteMode`（`initial_build/rebuild/incremental_update`）
- 新增可替换 embedding provider 接口：`backend/app/services/embedding_provider.py`
  - 抽象接口 `EmbeddingProvider`
  - 默认占位实现 `MockEmbeddingProvider`
- 新增向量仓储接口与内存实现：`backend/app/repositories/vector_repository.py`
  - 抽象接口 `VectorRepository`
  - 内存实现 `InMemoryVectorRepository`
  - 实现 cosine 相似度查询
- 新增流程服务骨架：
  - `backend/app/services/vectorization_service.py`：chunk -> vector record
  - `backend/app/services/vector_store_service.py`：写入/重建/增量更新/查询编排
- 新增基础测试：
  - `backend/tests/unit/test_vectorization_schema.py`
  - `backend/tests/unit/test_vector_store_service.py`
- 新增共享规范文档：
  - `docs/specs/03_向量化与向量存储.md`

## 第二轮新增/修改内容（向量化真实接入层升级）
- 升级配置中心：`backend/app/core/config.py`
  - 新增 embedding 接入配置：
    - `embedding_provider`
    - `embedding_model`
    - `embedding_dim`
    - `embedding_api_key`
    - `embedding_base_url`
    - `embedding_request_timeout_ms`
    - `embedding_provider_enable_real`
    - `embedding_provider_fallback_to_mock`
  - 新增向量仓储接入配置：
    - `vector_repository`
    - `vector_repository_enable_real`
    - `vector_repository_fallback_to_in_memory`
    - `vector_db_url`
    - `vector_table_name`
- 升级 provider 抽象：`backend/app/services/embedding_provider.py`
  - 新增 `EmbeddingProviderType`、`EmbeddingProviderConfig`
  - 新增构建入口 `build_embedding_provider(...)`
  - 新增真实接入位 `OpenAICompatibleEmbeddingProvider`（边界已定义，接线待后续）
  - 保留 `MockEmbeddingProvider` 作为默认 fallback
- 升级 repository 抽象：`backend/app/repositories/vector_repository.py`
  - 新增 `VectorRepositoryType`、`VectorRepositoryConfig`
  - 新增构建入口 `build_vector_repository(...)`
  - 新增真实接入位 `PgVectorRepository`（边界已定义，接线待后续）
  - 保留 `InMemoryVectorRepository` 作为默认 fallback
- 新增统一接入工厂：`backend/app/services/vector_access_factory.py`
  - 通过配置统一构建 provider/repository/vectorization/vector_store
  - 实现“可配置切换 + fallback”运行时骨架
- 接入消费方改造：
  - `backend/app/services/qa_pipeline_service.py` 改为通过工厂构建向量能力
  - `backend/app/services/offline_evaluator.py` 改为通过工厂构建向量能力
- 更新环境变量示例：`backend/.env.example`
- 新增测试：
  - `backend/tests/unit/test_vector_access_factory.py`（覆盖切换与 fallback）

## 对外变更
- 03 模块对外契约已落地并文档化，04 可直接消费 `VectorQueryHit`（含 `chunk_id`、`score_vector`、`citation`）。
- 建立了 provider 与 repository 的可替换接口，不锁定具体实现。
- 明确了三类写入流程模式及其骨架行为。
- 保持 04 已消费的 `VectorQueryHit` 结构不变。
- 保持 02 chunk 输入契约不变。
- 新增“真实接入位”但未破坏现有 mock/in-memory 本地可运行路径。

## 测试/验证结果
- 新增单测覆盖：
  - 输入契约继承 02 输出契约
  - 向量记录关键关联字段
  - initial build 全量写入
  - rebuild 覆盖旧向量
  - incremental update 按变更更新并清理 stale 数据
  - 向量查询返回 `score_vector`
- 第二轮验证：
  - `pytest backend/tests/unit/test_vector_access_factory.py backend/tests/unit/test_vectorization_schema.py backend/tests/unit/test_vector_store_service.py backend/tests/unit/test_hybrid_retrieval_service.py`
  - `pytest backend/tests/unit`
  - 结果：通过（23 passed）

## 风险与未覆盖项
- 当前默认 provider 为 mock，不代表真实模型效果与线上延迟。
- 仓储当前为内存实现，未覆盖持久化、并发控制、事务与重试。
- 尚未接入任务编排层（worker）与 API 暴露层，仅完成 schema + service + repository 骨架。
- 尚未进行性能压测与模型选型对比（任务 03 后续项）。
- `OpenAICompatibleEmbeddingProvider` 与 `PgVectorRepository` 当前仅完成接入边界，尚未接通真实 SDK/数据库操作。
- 尚未覆盖真实仓储的连接池、事务、批量 upsert SQL、索引管理与重试策略。
- 尚未覆盖真实 embedding provider 的限流、超时、批量切分与可观测性日志。

## 待确认项
- 是否要求向量记录强制补充 `page_no/page_range` 字段。
- 生产默认 embedding 模型与维度基线。
- 向量持久化首选落地方案（Supabase pgvector 或其他）及索引策略基线。
- 真实 provider 的优先接入标准（OpenAI 兼容接口、Supabase Edge Function，或直连第三方服务）。
- 真实仓储的最终主键/唯一约束方案（`vector_id` vs `kb_id+doc_id+chunk_id+model`）。
