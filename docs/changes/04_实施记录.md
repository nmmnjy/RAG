# 04 实施记录

## 当前状态
- 状态：进行中（已完成设计 + 代码骨架 + 基础测试骨架）

## 本线程新增/修改内容
- 新增混合检索与重排 schema：`backend/app/schemas/retrieval.py`
  - 定义关键词检索契约：`KeywordQueryRequest`、`KeywordQueryHit`
  - 定义混合检索契约：`HybridRetrieveRequest`、`HybridRetrieveHit`、`HybridRetrieveResult`
  - 定义融合配置：`HybridFusionConfig`、`ScoreNormalizationMethod`
  - 定义重排请求：`RerankRequest`
- 新增关键词检索模块骨架：`backend/app/services/keyword_retriever.py`
  - 抽象接口 `KeywordRetriever`
  - 默认实现 `InMemoryKeywordRetriever`（TF-IDF 风格评分）
- 新增融合策略模块：`backend/app/services/hybrid_fusion.py`
  - `min-max` 归一化默认实现
  - 加权融合默认实现
- 新增重排接口骨架：`backend/app/services/reranker.py`
  - 抽象接口 `Reranker`
  - 默认实现 `NoopReranker`
- 新增默认可运行混合检索流程：`backend/app/services/hybrid_retrieval_service.py`
  - 直接消费 03 `VectorQueryHit`
  - 串联语义召回 + 关键词召回 + 融合 + 可选重排
  - 输出可被 05 直接消费的稳定命中字段
- 新增 04 基础单测：`backend/tests/unit/test_hybrid_retrieval_service.py`

## 共享文档更新
- 新增共享规范：`docs/specs/04_混合检索与重排.md`
  - 明确 04 边界与冻结输入来源（03）
  - 明确关键词/混合输出 schema
  - 明确归一化与融合默认实现
  - 明确重排接口与默认行为
- 新增变更记录：`docs/changes/04_实施记录.md`（本文）

## 对外契约确认
- 严格复用 03 语义检索命中契约 `VectorQueryHit`，未引入第四套语义命中结构。
- 混合检索最终命中稳定字段至少包含：
  - `chunk_id`
  - `score_vector`
  - `score_keyword`
  - `score_final`
  - `citation`
  - `content`（供 05 直接生成答案上下文）

## 测试/验证结果
- 已补充 04 单测骨架，覆盖：
  - 关键词检索结果基础断言
  - 分数归一化与融合函数断言
  - 默认 hybrid 检索流程结果字段断言
- 具体执行结果见本轮 `pytest` 定向测试输出。

## 风险与未覆盖项
- 当前关键词检索为内存实现，未覆盖倒排索引持久化与大规模性能行为。
- 当前重排默认 `Noop`，未覆盖模型重排效果与延迟评估。
- 当前融合权重为全局默认值（0.6/0.4），未实现按知识库动态配置策略。
- 当前仅完成服务层骨架，尚未接入 API/任务编排层。

## 待确认项（仅提出，不改上游基线）
- 02/03 是否需要补充页码级标准字段（如 `page_no/page_range`）以增强 citation 粒度。
- 04 阶段是否要求固定关键词检索线上引擎（BM25/ES/PG trigram）作为默认实现。
- 04 阶段是否要求默认启用轻量重排模型，而非 `NoopReranker`。
