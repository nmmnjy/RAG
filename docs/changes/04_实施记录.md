# 04 实施记录

## 当前状态
- 状态：第三轮收敛完成（契约冻结 + 融合收敛 + 重排回退 + 评测样例 + 测试补齐）

## 本线程新增/修改内容
- 配置与契约收敛：
  - 更新 `backend/app/schemas/retrieval.py`
  - 冻结输出字段结构不变（`chunk_id/score_vector/score_keyword/score_final/citation`）
  - `vector_top_k/keyword_top_k` 调整为 `>=0`，支持显式关闭通道
  - 保留旧顶层参数并通过 `get_effective_tuning_config()` 统一解析
- 融合与排序收敛：
  - 更新 `backend/app/services/hybrid_retrieval_service.py`
  - 收敛规则：
    - `vector_top_k=0` 跳过语义召回
    - `keyword_top_k=0` 跳过关键词召回
    - 同分排序按 `chunk_id` 稳定化
  - 新增重排失败回退：
    - `enable_rerank=true` 且重排异常 -> 回退未重排结果
    - `debug` 输出 `rerank_status/rerank_error`
- 关键词检索稳定化：
  - 更新 `backend/app/services/keyword_retriever.py`
  - 支持 `top_k=0` 直接返回空
  - 同分排序按 `chunk_id` 稳定化
- 离线样例与测试补齐：
  - 新增 `backend/tests/fixtures/04_hybrid_retrieval/hybrid_eval_cases_v1.json`
  - 更新 `backend/tests/unit/test_hybrid_retrieval_service.py`，新增：
    - 纯语义/纯关键词/混合三路径测试
    - 融合排序稳定性测试（同分稳定）
    - rerank 失败回退测试
    - “混合较纯关键词提升”最小复现实验测试
- 文档同步：
  - 更新 `docs/specs/04_混合检索与重排.md`
  - 更新 `docs/changes/04_实施记录.md`（本文）

## 测试/验证结果
- `pytest backend/tests/unit/test_hybrid_retrieval_service.py -q`：`9 passed`
- `pytest backend/tests/unit/test_vector_store_service.py -q`：`3 passed`

## 输出契约确认
- 未修改 03 `VectorQueryHit` 契约。
- 未修改 05 消费的 04 输出字段结构。
- 04 输出关键稳定字段保持：
  - `chunk_id`
  - `score_vector`
  - `score_keyword`
  - `score_final`
  - `citation`

## 风险与待确认项
- 风险：
  - 当前离线提升样例为最小构造集，不能代表真实业务语料分布。
  - 默认 `NoopReranker` 仍为占位实现，线上收益待真实 reranker 验证。
  - 关键词检索仍为内存实现，性能与召回上限待后续引擎接入。
- 待确认：
  - 下一轮是否固定线上关键词检索基线（BM25/ES/PG trigram）。
  - 下一轮是否引入默认轻量 reranker 并进入离线评测对比。
