# 04 实施记录

## 当前状态
- 状态：进行中（第一轮骨架已完成；第二轮完成“效果增强准备”）

## 本线程新增/修改内容
- 配置位整理（不破坏原调用）：
  - 更新 `backend/app/schemas/retrieval.py`
  - 新增 `HybridRetrieveTuningConfig`，集中承载：
    - `top_k`
    - `vector_top_k`
    - `keyword_top_k`
    - `enable_rerank`
    - `fusion_config`
  - `HybridRetrieveRequest` 新增 `tuning_config`，并提供 `get_effective_tuning_config()`
  - 保留原顶层字段，兼容旧路径
- 融合策略可配置骨架：
  - 更新 `backend/app/services/hybrid_fusion.py`
  - 新增 `FusionStrategy` 抽象
  - 新增默认策略 `WeightedSumFusionStrategy`
  - 新增策略注册表与 `resolve_fusion_strategy()`
  - 保持 `fuse_scores(...)` 入口不变，默认仍可运行
- 检索流程接入统一调优配置：
  - 更新 `backend/app/services/hybrid_retrieval_service.py`
  - 使用 `get_effective_tuning_config()` 驱动 `top_k/vector_top_k/keyword_top_k/enable_rerank/fusion_config`
  - `debug` 新增：`fusion_strategy`、`enable_rerank`、`reranker_name`
- reranker 边界清晰化：
  - 更新 `backend/app/services/reranker.py`
  - `Reranker` 新增 `reranker_name`
  - 在接口注释中明确：不改写 `chunk_id/score_vector/score_keyword` 语义
  - 默认 `NoopReranker` 不变
- 测试补充：
  - 更新 `backend/tests/unit/test_hybrid_retrieval_service.py`
  - 新增“权重变化影响排序”测试
  - 新增“tuning_config.top_k 影响候选数量”测试

## 现在可配置的参数
- 请求级：
  - `top_k`
  - `vector_top_k`
  - `keyword_top_k`
  - `enable_rerank`
- 融合级：
  - `fusion_config.strategy_name`（当前实现 `weighted_sum`）
  - `fusion_config.normalization_method`（当前实现 `min_max`）
  - `fusion_config.vector_weight`
  - `fusion_config.keyword_weight`

## 仍为占位实现的能力
- `NoopReranker` 仍是默认实现（接口可替换，效果不增强）。
- 关键词检索仍为内存实现（未接入线上倒排/搜索引擎）。
- 融合策略当前仅实现 `weighted_sum`，已具备扩展骨架。

## 兼容性与契约确认
- 未修改 `03` 向量命中契约（`VectorQueryHit`）。
- 未修改 `05` 已消费的混合检索结果结构（`HybridRetrieveHit` 核心字段保持不变）。
- 本地 mock/in-memory 路径保持可运行。

## 测试/验证结果
- `pytest backend/tests/unit/test_hybrid_retrieval_service.py -q` 通过（5 passed）

## 风险与待确认项
- 风险：
  - 参数可调后，缺少统一离线调参流程时容易出现“局部最优配置漂移”。
  - reranker 接口虽可替换，但尚未接入真实模型重排，无法验证真实收益。
- 待确认：
  - 是否在 04 下一步固定“默认线上关键词检索方案”（BM25/ES/PG trigram）。
  - 是否在 04 下一步引入轻量级默认 reranker，替代 noop。
