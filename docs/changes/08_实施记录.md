# 08 实施记录

## 当前状态
- 状态：第二轮进行中（已完成目录收敛 + 本地可演示运行手册收敛）

## 本轮实现
- 已将 `ops/` 下部署与运维骨架全量迁移到 `infra/`。
- 已清理 `ops/` 目录，消除双目录基线。
- 已修正所有已识别文档与脚本中的 `ops/*` 路径引用到 `infra/*`。
- 已补齐 `docs/specs/08_部署与运维.md` 的收敛后状态说明。
- 新增本地可演示脚本：
  - `infra/scripts/local-frontend.ps1`
  - `infra/scripts/local-backend.ps1`
  - `infra/scripts/local-evaluation.ps1`
  - `infra/scripts/local-acceptance.ps1`
- 增强环境预检脚本：`infra/scripts/preflight.ps1` 支持 `core/vector/qa/all` profile（对齐 00/03/05）。
- 新增本地演示运行手册：`infra/runbooks/local-demo-runbook.md`。
- 新增本地演示环境模板：`infra/environments/.env.local-demo.example`。

## 实际迁移内容
- `ops/vercel/*` -> `infra/vercel/*`
- `ops/backend/*` -> `infra/supabase/*`
- `ops/environments/*` -> `infra/environments/*`
- `ops/scripts/*` -> `infra/scripts/*`
- `ops/topology/*` -> `infra/topology/*`
- `ops/monitoring/*` -> `infra/monitoring/*`
- `ops/runbooks/*` -> `infra/runbooks/*`
- `ops/README.md` -> `infra/README.md`

## 文档修正内容
- 更新 `docs/specs/00_项目框架与规范.md` 的 `infra/` 子目录蓝图。
- 重写 `docs/specs/08_部署与运维.md` 为收敛后事实状态（移除 `ops` 决策草案）。
- 更新 `docs/modules/08_部署与运维_实施说明.md` 路径引用为 `infra/*`。
- 本记录文档同步更新为“迁移完成”状态。
- 补充 `docs/specs/08_部署与运维.md` 的“本地一键检查流程”和“00/03/05 环境变量对齐”章节。

## 目录映射歧义与最终选择
- 歧义点：`ops/backend` 映射到 `infra/backend` 还是 `infra/supabase`。
- 最终选择：映射到 `infra/supabase`，理由是与 `docs/specs/00_项目框架与规范.md` 现有蓝图保持一致。

## 风险与阻塞
- 历史分支或未合并分支中可能仍存在 `ops/*` 路径引用，合并时需再做一次全仓扫描。
- 当前为目录与文档收敛，不包含真实上线验证，部署可用性仍依赖后续环境接入。
- 本地一键检查依赖开发机已有 Node/Python 与依赖安装；若环境未准备好会导致脚本失败。
- 离线评测结果受样本数据和 mock provider 影响，不代表生产质量上限。

## 第二轮验收快照（脚本化）
- 前端构建命令：`infra/scripts/local-frontend.ps1 -Action build`
- 后端关键测试命令：`infra/scripts/local-backend.ps1 -Action test -TestScope key`
- 离线评测命令：`infra/scripts/local-evaluation.ps1 -Dataset qa_demo_v1`
- 一键执行命令：`infra/scripts/local-acceptance.ps1 -EvalDataset qa_demo_v1`
- 报告查看目录：`evaluation/reports/`
- 本机执行结果（2026-03-29）：
  - `preflight(all)`：通过
  - 后端关键测试：通过（8 passed）
  - 离线评测：通过（`release_blocked=False`，生成 `qa_demo_v1_*.json`）
  - 前端构建：失败（`spawn EPERM`，已记录在 runbook 故障排查）

## 下一步
- 主线程合并后执行一次全仓 `ops/` 关键字扫描，确保无残留引用。
- 在 CI/CD 中按 `infra/scripts/preflight.ps1` 和 `infra/scripts/health-check.ps1` 接入发布校验。
- 将 `infra/scripts/local-acceptance.ps1` 接入主线程的“第二轮验收快照”流水线。
